import baseService from "exos-core";
import { Item } from "./types";

async function getItems(): Promise<Item[]> {
  const url = "/items";
  return baseService.get<Item>(url);
}

export {
  getItems
}
